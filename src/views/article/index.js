import React from 'react'
import { withRouter } from 'react-router'
import { createHashHistory } from 'history'

import './article.scss'

const Article = props => {
  return (
    <section>

    </section>
  )
}